## 2026-07-06

### 从零搭建汽保工具全栈网站
- 使用 Next.js 14 (App Router) + TypeScript + Tailwind CSS 构建
- Prisma ORM + SQLite 数据库，种子数据包含 6 分类 6 产品
- iron-session 管理后台认证
- 完整前台：首页(Hero+分类导航+推荐产品)、产品列表(筛选/搜索/分页)、产品详情(图片轮播+规格表)、关于我们、联系我们
- 完整后台：登录、仪表盘、产品CRUD、分类CRUD(弹窗)、网站设置
- 构建成功：20个静态页面 + 8个API路由 + 中间件
- 设计风格：现代简约(深蓝+白色+橙色)，响应式适配移动端
- 已配置 vercel.json 用于 Vercel 免费部署
- 待部署：需注册 Vercel + GitHub 账号，连接 Neon PostgreSQL

### 后台布局与移动端适配调整
- 使用 Next.js Route Group 重构后台目录：admin/layout.tsx 轻量包装，admin/(dashboard)/layout.tsx 包含侧边栏
- 登录页独立显示，不显示侧边栏和前台导航
- 使用 LayoutWrapper 客户端组件条件渲染 Header/Footer（后台页面隐藏）
- AdminSidebar 增加移动端汉堡菜单 + 遮罩层 + 滑入动画
- 所有后台页面 padding 改为 p-4 md:p-8 适配手机
- 表格添加 overflow-x-auto 和 min-w 保证移动端横向滚动
- 表单 grid 改为 grid-cols-1 sm:grid-cols-2
- 分页组件添加 flex-wrap 适配窄屏
- AdminHeader 移动端标题左边距适配汉堡按钮

### 公司名称更新
- 全局品牌名改为"赤赫汽保工具"，覆盖 Header/Footer/Sidebar/Login/Metadata/Seed

### 代码审计与安全修复
- 修复所有严重+高危问题：middleware API保护纳入/api/*、移除登录页默认凭据、会话密钥强校验、slugify支持中文、zod入参校验(5个API)、分页limit上限100、移除SVG上传(XSS)、DELETE P2025错误返回404、settings白名单、登录5次失败锁1分钟限速
- 中等问题：仪表盘区分总数/已发布/loading、Toast组件、ImageUpload res.ok、空文件校验、首页加try/catch
- 体验优化：contact/footer/about从settings API动态读取、FeaturedProducts改用Link、产品列表/详情增加error+retry、layout加OG/favicon、ProductGallery产品名alt、全局loading.tsx
- 新增文件：validation.ts、Toast.tsx、hooks.ts、loading.tsx

### 管理员修改密码功能
- 新增 API: `POST /api/auth/change-password` (验证旧密码 → 哈希新密码 → 更新DB)
- 新增页面: `admin/(dashboard)/change-password/page.tsx` (旧密码+新密码+确认密码表单)
- validation.ts 新增 `changePasswordSchema` (zod校验 + 两次密码一致性校验)
- AdminSidebar 新增"修改密码"菜单项
