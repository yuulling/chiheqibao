## 2026-07-10

### 联系邮箱字段残留问题
- 发现后台设置页仍显示「联系邮箱」字段（已在前一次 commit 删除，但浏览器/Edge 缓存导致未生效）
- 已确认 `SettingsForm.tsx` / `validation.ts` / `Footer.tsx` 中均无联系邮箱
- 在 `next.config.js` 中为 `/admin/:path*` 和 `/api/:path*` 添加 `Cache-Control: no-store` 等禁用缓存头
- 推送新 commit 触发 Vercel 重新部署，确保设置表单更新及时生效
